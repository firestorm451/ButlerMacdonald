<div class="col-lg-4 col-md-4">
  <%= simple_form_for :session, url: login_create_path, html: { class: 'form-horizontal' } do |f| %>
    <%= f.input :name, autofocus: true %>
    <%= f.input :password %>
    <%= f.button :submit, "Submit", id: 'login_user' %>
  <% end %>
</div>
